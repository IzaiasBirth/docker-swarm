#tokens que estabelecem o cluster, definindo os Managers e Workers; usar os códigos gerados na tela pelo comando 'docker swarm init'

# Worker
docker swarm join --token ...

# Manager
docker swarm join --token ...